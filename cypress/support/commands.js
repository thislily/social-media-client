// // cypress/support/commands.js

// // Custom command for selecting elements by a more general CSS selector
// Cypress.Commands.add('getBySelector', (selector) => {
//     return cy.get(selector);
//   });
  